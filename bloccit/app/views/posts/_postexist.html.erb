 <% if @user.posts.count && @user.comments.count > 0 %>
    <h2>Posts</h2>
      <%= render @user.posts %>
 
    <h2>Comments</h2>
      <%= render @user.comments %>
 <% else %>
   "{user.name} has not submitted any posts yet."
 <% end %>